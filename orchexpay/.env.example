# Optional: override API base URLs when not using Vite dev proxy (e.g. production or different host).
# Leave unset to use relative paths: '' and '/payout-api' (dev server proxies /api, /auth → 8080, /payout-api → 8081).
# VITE_WALLET_API=http://localhost:8080
# VITE_PAYOUT_API=http://localhost:8081
